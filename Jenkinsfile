pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'uname -a'
                echo 'echo this is the master branch'
            }
        }
    }
}

